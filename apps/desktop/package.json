{
  "name": "@astrlink/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "branding:sync": "bun scripts/sync-branding.mjs",
    "dev": "bun run branding:sync && tsc --noEmit && rsbuild dev",
    "build": "bun run branding:sync && tsc --noEmit && rsbuild build",
    "check": "bun run branding:sync && tsc --noEmit && vitest run --maxWorkers=1 && rsbuild build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --maxWorkers=1",
    "test:watch": "vitest --maxWorkers=1",
    "tauri": "tauri",
    "sidecar:build": "bun scripts/build-sidecar.mjs",
    "desktop:dev": "bun run branding:sync && bun run sidecar:build && tauri dev",
    "desktop:build": "bun run branding:sync && bun run sidecar:build && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.31.0",
    "radix-ui": "^1.6.7",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "sonner": "^2.0.8",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@rsbuild/core": "2.1.7",
    "@rsbuild/plugin-react": "2.1.0",
    "@tailwindcss/postcss": "^4.3.3",
    "@tauri-apps/cli": "2.11.4",
    "@types/node": "22.20.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "happy-dom": "20.8.3",
    "postcss": "^8.5.26",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "typescript": "7.0.2",
    "vitest": "4.1.0"
  }
}
